const power = (x, n) => {
  let result = 1;
  for (let i = 0; i < n; i++) {
    result *= x;
  }
  return result;
};

// Пример использования:
const x = 2;
const n = 5;
const result = power(x, n);
console.log(result);
